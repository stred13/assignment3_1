server:
  port: 8082
spring:
  application:
    name: assignment3_1
  datasource:
    url: jdbc:postgresql://localhost:5432/bookdb2?useSSL=false&serverTimezone=UTC
    username: xsb
    password: xsb-server
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
